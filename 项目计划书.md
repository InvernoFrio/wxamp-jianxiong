# 健雄书韵，码绘初心 — 微信小程序项目计划书

## 一、项目概述

| 项目 | 内容 |
|------|------|
| 名称 | **健雄书韵** |
| 定位 | 吴健雄数字纪念馆 —— 以"书韵"为魂，以代码为笔 |
| 技术 | 微信小程序（原生开发） |
| 团队 | 3 人 |
| 后端 | 云开发（轻量后端） |

### 核心理念

> "书韵"不只是读书，是用数字化的语言重新书写一位伟大科学家的一生。

小程序以「翻开一本书」的意象贯穿始终：首页是一本悬浮的书，翻开后左页是地图探索入口，右页是直接阅读入口。用户翻阅的不是文字，而是时间线、实验、笔记——科学与人文的交汇。

---

## 二、特色功能总览

| 编号 | 功能 | 说明 |
|------|------|------|
| **A** | 生平年表 | 交互式时间轴（1912—1997） |
| **B** | 书韵阅读 | 章节化阅读 + 批注 + 进度保存 |
| **C** | 物理之光 | 宇称不守恒实验 + 气体扩散实验 + β衰变 |
| **D** | 影音流年 | 视频 + 沉浸式相册 + 背景音乐 |
| **E** | AI 对话 | 不同地点与"吴健雄"对话，上下文注入 |
| **F** | 打卡系统 | 校园地点打卡 + 实景二维码预留，以走格子为载体 |

---

## 三、功能架构

```
健雄书韵（小程序）
│
├── 🏠 首页 · 书卷
│   ├── 3D 翻书动画（封面 → 探索地图页 / 直接阅读页）
│   ├── 动态名言库：支持多条名言轮播与随机展示
│   ├── 翻书状态联动：控制全局 UI 的淡入淡出（名言、菜单、AI 岛）
│   └── 底栏：书卷 / 阅读 / 年表 / 物理 / 影音
│
├── 🗺️ 走格子 · 探索地图（特色功能 F）
│   ├── 校园地图：以南京大学为背景的图结构探索
│   ├── 路线系统：求学之路 / 传承之路 / 探索之路
│   ├── 节点互动：故事、阅读、实验、翻书等多种节点类型
│   ├── 打卡系统：校园地点打卡 + 实景二维码预留
│   ├── 进度持久化：已探索节点、已完成路线本地保存
│   └── 内容跳转：节点关联阅读章节 / 年表 / 物理实验
│
├── 📖 第一章 · 生平年表（功能 A）
│   ├── 交互式时间轴（1912—1997）
│   ├── 时期锚点导航：快速跳转至"求学"、"曼哈顿计划"等特定时期
│   ├── 里程碑事件卡片（液态玻璃风格）
│   ├── 事件详情弹窗：点击时间点展示详细文字与珍贵照片
│   └── 关键节点标注（诺贝尔奖、宇称实验等）
│
├── 📚 第二章 · 书韵阅读（功能 B）
│   ├── 章节化阅读：支持长篇传记的分章节加载
│   ├── 阅读进度持久化：自动保存并同步用户的阅读位置
│   ├── 交互式批注：在特定段落显示历史背景补充
│   ├── 划线高亮 + 批注功能
│   └── 读书笔记列表
│
├── 🧪 第三章 · 物理之光（功能 C）
│   ├── 实验模拟引擎
│   │   ├── 宇称不守恒：模拟极低温环境下的粒子偏转
│   │   ├── 气体扩散：交互式演示同位素分离过程
│   │   └── β衰变：粒子轨迹可视化（核心）
│   ├── 科学原理解析：图文并茂的学术背景介绍
│   └── 交互式操作引导
│
├── 🎬 第四章 · 影音流年（功能 D）
│   ├── 视频流媒体：集成腾讯视频插件或原生视频组件播放纪录片
│   ├── 沉浸式相册：支持大图预览、缩放与历史背景解说
│   └── 背景乐赏：全局或页面内的音频播放控制
│
├── 💬 AI 对话（功能 E）
│   ├── 不同地点触发不同对话场景（上下文注入）
│   ├── 基于大模型的问答系统
│   └── 支持流式或分段回复
│
├── 👤 关于
│   ├── 致敬词：关于本项目的初衷与愿景
│   ├── 团队介绍
│   └── 项目说明
│
└── 🎨 全局设计系统
    ├── 书韵主题（宣纸纹理 + 墨色 + 朱红点缀）
    ├── 液态玻璃 UI 组件库
    └── 翻页/淡入动画系统
```

---

## 四、数据接口规范

### 4.1 首页 (Home - 书卷)

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/quotes` | GET | 获取名言列表 |
| `/api/ai/chat` | POST | 发送对话请求 |

**名言列表返回：**
```json
Array<{ id: string, text: string, source: string }>
```

**AI 对话：**
- 输入：`{ message: string, history: Array<ChatHistory> }`
- 返回：`{ reply: string, suggestions: string[] }`

### 4.2 生平年表 (Timeline - 年表)

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/timeline` | GET | 获取完整年表数据 |

**返回结构：**
```json
Array<{
  year: string,
  title: string,
  content: string,
  images: string[],
  category: string
}>
```

### 4.3 书韵阅读 (Reader - 阅读)

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/book/chapters` | GET | 获取书籍章节目录 |
| `/api/book/content/{chapterId}` | GET | 获取特定章节正文 |
| `/api/user/progress` | POST | 上传阅读进度 |

**章节目录返回：**
```json
Array<{ chapterId: string, title: string, summary: string }>
```

**章节正文返回：**
```json
{
  content: string,
  annotations: Array<{ offset: number, text: string }>
}
```

### 4.4 物理之光 (Physics - 物理)

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/physics/experiments` | GET | 获取实验列表及状态 |
| `/api/physics/simulation/{type}` | GET | 获取实验模拟所需的物理参数配置 |

**实验列表返回：**
```json
Array<{
  type: string,
  title: string,
  difficulty: string,
  completed: boolean
}>
```

### 4.5 影音流年 (Multimedia - 影音)

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/media/videos` | GET | 获取视频档案 |
| `/api/media/gallery` | GET | 获取历史相册 |

**视频档案返回：**
```json
Array<{
  id: string,
  title: string,
  cover: string,
  url: string,
  duration: string
}>
```

**相册返回：**
```json
Array<{
  id: string,
  url: string,
  title: string,
  location: string,
  date: string
}>
```

### 4.6 关于 (About)

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/about/info` | GET | 获取关于页面的文案与版本信息 |

---

## 五、UI 设计规范

### 5.1 色彩系统 —— "书韵"

| 角色 | 色值 | 用途 |
|------|------|------|
| **宣纸白** | `#F5F0E8` | 背景底色 |
| **墨黑** | `#2C2C2C` | 正文文字 |
| **朱红** | `#C41E3A` | 强调色、标题、进度条 |
| **青灰** | `#8B9DAF` | 次要文字、边框 |
| **琥珀** | `#D4A574` | 时间轴节点、勋章 |
| **玻璃白** | `rgba(255,255,255,0.25)` | 磨砂玻璃背景 |
| **玻璃边光** | `rgba(255,255,255,0.6)` | 液态玻璃边缘高光 |

### 5.2 液态玻璃组件规范

**磨砂按钮（Glass Button）：**
```
背景：rgba(255,255,255,0.18) ~ 0.25
模糊：backdrop-filter: blur(12px)
圆角：16rpx
边框：1rpx solid rgba(255,255,255,0.5)（边缘光）
阴影：0 4rpx 16rpx rgba(0,0,0,0.08)
按态：轻微缩放 0.97 + 亮度提升
```

**磨砂卡片（Glass Card）：**
```
背景：rgba(255,255,255,0.22)
模糊：backdrop-filter: blur(16px)
圆角：24rpx
边框：1rpx solid rgba(255,255,255,0.4)
内阴影：inset 0 1rpx 0 rgba(255,255,255,0.6)（顶部边缘光）
```

### 5.3 动画规范

| 场景 | 动画 | 时长 |
|------|------|------|
| 页面进入 | 内容从下方淡入上滑 | 400ms ease-out |
| 卡片出现 | 缩放 + 淡入（0.92→1） | 350ms ease-out |
| 时间轴节点 | 脉冲光晕 | 600ms |
| 翻页 | 水平滑动 | 300ms ease-in-out |
| 按钮点击 | scale(0.97) | 100ms |

### 5.4 字体

- 标题：`"Source Han Serif SC"`（思源宋体）或系统 serif
- 正文：系统默认 sans-serif
- 英文/数字：`"SF Pro"` 风格

---

## 六、页面详细设计

### 6.1 首页 · 书卷

**布局：**
- 全屏背景：宣纸纹理 + 淡淡的水墨晕染
- 中央：一本悬浮的 3D 书（CSS 变换模拟透视），书封写"健雄书韵"
- 底部：四个书签式玻璃卡片，从下向上滑入
  - 「生平年表」「书韵阅读」「物理之光」「关于」
- 顶部：吴健雄名言，每 5 秒淡入切换
- AI 对话入口：「AI 岛」悬浮按钮

**交互：**
- 点击书 → 翻书动画 → 展开四个模块
- 点击书签卡片 → 进入对应模块（页面转场：书页翻动）
- 点击 AI 岛 → 进入智能对话界面

### 6.2 第一章 · 生平年表

**布局：**
- 竖向时间轴（左侧一条朱红线）
- 右侧事件卡片（液态玻璃风格）
- 关键节点有特殊标记（⭐ 诺贝尔、🎓 学位、🏛️ 重要机构）
- 顶部：时期锚点导航（求学 / 曼哈顿计划 / 研究 / 荣誉）

**年表数据结构：**
```js
{
  year: 1912,
  title: "出生于江苏太仓",
  detail: "吴健雄出生于江苏省太仓市浏河镇...",
  type: "birth", // birth | education | research | award | other
  image: "" // 可选配图
}
```

**交互：**
- 上下滑动浏览
- 点击卡片 → 展开详情弹窗（动画展开）
- 顶部筛选标签：全部 / 教育 / 研究 / 荣誉
- 时期锚点快速跳转

### 6.3 第二章 · 书韵阅读

**布局：**
- 顶部：章节导航（横向滚动标签）
- 正文区：仿书籍排版（两端对齐、首行缩进）
- 右侧：细滚动条 + 阅读进度百分比

**功能：**
- **章节化阅读**：支持长篇传记的分章节加载
- **阅读进度持久化**：自动保存并同步用户的阅读位置
- **划线高亮**：长按文字 → 选中 → 标记高亮（朱红色下划线）
- **批注**：选中后弹出「写笔记」→ 输入框 → 保存
- **交互式批注**：在特定段落显示历史背景补充
- **笔记列表**：侧边栏展示所有批注，点击跳转到原文位置

**内容结构：**
```
chapters/
├── ch01-birth.json    # 少年时代（太仓→苏州）
├── ch02-education.json # 求学（中央大学→伯克利）
├── ch03-research.json  # 研究生涯（β衰变）
├── ch04-parity.json    # 宇称不守恒实验
├── ch05-honor.json     # 荣誉与晚年
└── ch06-legacy.json    # 精神遗产
```

### 6.4 第三章 · 物理之光

**核心：实验模拟引擎**

#### 实验一：宇称不守恒

用 Canvas 2D 绘制一个简化版的实验场景：
- 钴-60 原子核（圆球，带放射性标记）
- β粒子发射（动画粒子沿不同方向飞出）
- 磁场线（弯曲的弧线）
- 探测器环（围绕原子核的半圆）

**交互流程：**
1. 介绍页面：实验背景（为什么做这个实验）
2. 概念页：宇称守恒是什么（对称性动画：镜像对比）
3. 实验页：点击「开始实验」→ 播放粒子动画
4. 结论页：展示不对称结果 → 宇称不守恒

#### 实验二：气体扩散

交互式演示同位素分离过程：
- 气体分子运动动画
- 扩散膜穿透模拟
- 浓度变化可视化

**辅助页面：**
- 实验原理图解（静态 SVG/CSS 图）
- 吴健雄实验的创新之处（文字 + 示意图）

### 6.5 第四章 · 影音流年

**视频播放：**
- 集成腾讯视频插件或原生视频组件
- 播放吴健雄相关纪录片

**沉浸式相册：**
- 历史照片大图预览
- 支持缩放与历史背景解说
- 时间线式浏览

**背景音乐：**
- 全局或页面内的音频播放控制
- 适合阅读时播放的轻音乐

---

## 七、技术架构

### 7.1 目录结构

```
jianxiong-reader/
├── app.js              # 全局逻辑
├── app.json            # 页面路由 + 全局配置
├── app.wxss            # 全局样式（液态玻璃组件库）
├── project.config.json # 项目配置
├── sitemap.json
│
├── assets/             # 静态资源
│   ├── textures/       # 宣纸纹理、水墨背景
│   ├── icons/          # 图标
│   └── images/         # 配图（待补充）
│
├── components/         # 自定义组件
│   ├── glass-card/     # 液态玻璃卡片
│   ├── glass-button/   # 液态玻璃按钮
│   ├── book-cover/     # 首页书本（多页翻书）
│   ├── map-canvas/     # 走格子地图 Canvas 渲染
│   ├── timeline-item/  # 时间轴节点
│   └── reader-view/    # 阅读器视图
│
├── pages/
│   ├── home/           # 首页 · 书卷
│   ├── grid-walking/   # 走格子 · 探索地图（功能 F）
│   ├── timeline/       # 第一章 · 生平年表（功能 A）
│   ├── reader/         # 第二章 · 书韵阅读（功能 B）
│   ├── physics/        # 第三章 · 物理之光（功能 C）
│   ├── multimedia/     # 第四章 · 影音流年（功能 D）
│   └── about/          # 关于
│
├── data/
│   ├── timeline.js     # 年表数据
│   ├── quotes.js       # 吴健雄名言
│   ├── map-data.js     # 走格子地图图数据（节点/边/路线）
│   └── chapters/       # 传记章节内容
│
└── utils/
    ├── storage.js      # 本地存储封装
    └── animation.js    # 动画工具
```

### 7.2 关键技术点

| 技术点 | 实现方式 |
|--------|----------|
| 液态玻璃效果 | `backdrop-filter: blur()` + 半透明背景 + 边框高光 |
| 翻书动画 | CSS `transform: perspective() rotateY()` + `transition` |
| 走格子地图 | Canvas 2D 图结构渲染 + 贝塞尔曲线连线 + 脉冲动画 |
| 打卡系统 | 以走格子为载体，节点关联实景二维码预留 |
| 时间轴 | 纵向 flex 布局 + 伪元素竖线 + 节点圆点 |
| 物理实验 | Canvas 2D + `requestAnimationFrame` 粒子动画 |
| 阅读器 | `rich-text` 组件 或 纯 `text` + `view` 布局 |
| 划线批注 | 长按事件 + `wx.showModal` + `wx.setStorageSync` |
| 数据持久化 | `wx.setStorageSync` / `wx.getStorageSync` |
| AI 对话 | 不同地点上下文注入 + 大模型 API（流式响应） |

### 7.3 兼容性

- 基础库版本：≥ 2.25.0（支持 `backdrop-filter`）
- 最低适配：iOS 12+ / Android 8+
- Canvas 使用 Canvas 2D（非旧版 Canvas）

---

## 八、团队分工（3 人）

### 建议分工

| 角色 | 负责模块 | 技能要求 |
|------|----------|----------|
| **A（俊贤）** | 物理实验可视化 + 数据整理 | Canvas 2D 动画、物理知识 |
| **B** | 生平年表 + 首页 | 布局、时间轴组件、翻书动画 |
| **C** | 书韵阅读器 + 关于页 | 文本处理、划线批注、存储 |

**公共部分（一起维护）：**
- 液态玻璃组件库（glass-card、glass-button）
- 色彩系统 & 全局样式
- 传记内容整理

### 开发顺序

```
第1步：搭建项目骨架（公共）
├── 创建项目结构
├── 全局样式 & 色彩变量
└── 液态玻璃基础组件

第2步：各模块并行开发
├── A → 物理实验
├── B → 时间轴 + 首页
└── C → 阅读器

第3步：联调 & 打磨（公共）
├── 页面跳转串联
├── 动画调优
└── 真机测试
```

---

## 九、开发排期

> 时间充裕，建议稳扎稳打

| 阶段 | 内容 | 预计时间 |
|------|------|----------|
| **P0 准备** | 项目计划确认、传记内容整理、素材收集 | 1-2 天 |
| **P1 骨架** | 项目创建、全局样式、玻璃组件、首页 | 2-3 天 |
| **P2 核心** | 时间轴、阅读器、物理实验（并行） | 5-7 天 |
| **P3 扩展** | 影音流年、AI 对话 | 2-3 天 |
| **P4 联调** | 页面串联、动画打磨、数据完善 | 2-3 天 |
| **P5 测试** | 真机测试、Bug 修复、细节优化 | 2-3 天 |
| **P6 交付** | 材料整理、展示准备 | 1-2 天 |

**总计约 3-4 周**（3 人并行，实际取决于传记内容到位时间）

---

## 十、开发建议

### 数据存储
由于是小程序，建议初期将静态数据（如年表、名言）放在本地 `data/` 文件夹中，后期再迁移至云开发数据库。

### 性能优化
物理实验模拟建议使用 Canvas 进行绘制，以保证流畅度。

### 全局状态
建议在 `app.js` 中维护一个 `globalData.readProgress`，确保跨页面进度同步。

---

## 十一、创新点总结（评审加分）

1. **"书韵"意象贯穿** — 不是功能堆砌，而是"翻开一本书"的完整叙事体验
2. **液态玻璃 UI** — 微信小程序中少见的高级质感，视觉辨识度高
3. **走格子打卡系统** — 校园地图探索 + 实景二维码，游戏化学习载体
4. **物理实验可视化** — 把抽象的宇称不守恒、气体扩散、β衰变变成可交互的动画
5. **阅读器 + 批注** — 数字化共读体验，功能实用且贴合比赛主题
6. **AI 场景对话** — 不同地点触发不同对话上下文，让用户"与吴健雄对话"
7. **影音流年** — 多媒体融合，沉浸式历史体验

---

## 十二、待俊贤提供的材料

- [ ] 《吴健雄传》相关内容（拍照/笔记均可，我来整理成结构化数据）
- [ ] 配图素材（吴健雄照片、实验场景、历史照片等）
- [ ] 视频素材（纪录片链接或文件）
- [ ] 背景音乐素材
- [ ] 确认分工：3 位成员各自负责哪个模块
- [ ] 确认项目名称是否就叫「健雄书韵」

---

*计划书由小满起草 · 2026-03-30*
*更新：2026-04-27 · 整理六大特色功能，新增打卡系统与走格子模块*
